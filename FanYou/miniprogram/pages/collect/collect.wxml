

<view class='container'>
  <view class='mrys' wx:for="{{storyList}}" wx:key="id">
    <view class='title'>{{item.city}}</view>
    <image class='image' src='{{item.img_url}}' mode='aspectFill'></image>
    
    <view class='content'>{{item.introduction}}</view>
  </view>


</view>